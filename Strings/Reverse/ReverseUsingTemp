#include <stdio.h>

int main()
{
    char temp,arr[]="Swapnaja";
    for(int i=0, j=(sizeof(arr)-2);i<j;i++,j--)
    {
     temp=arr[j];
     arr[j]=arr[i];
     arr[i]=temp;
    }
     printf("%s\n",arr);
}
