// using function

void reverse(int a[], int n)
{
    int i,t;
    for(i=0;i<n;i++)
    {
        t=a[i];
        a[i]=a[n-1];
        a[n-1]=t;
    }
}

int main()
{
    int a[10],b[10],i,j;
    
    for(i=0;i<10;i++)
    scanf("%d",&a[i]);
    
    reverse(a,10);
    
    for(i=0;i<10;i++)
    printf("%d ",a[i]);
}
